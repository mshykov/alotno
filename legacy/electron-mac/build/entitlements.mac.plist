<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!--
      Hardened-runtime entitlements for a notarized (Developer ID) Electron app.
      This is NOT a Mac App Store sandbox build, so no com.apple.security.app-sandbox.

      Electron/V8 require JIT and writable+executable memory. Without these the
      app crashes immediately under the hardened runtime.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!--
      sharp ships prebuilt native libraries (libvips). disable-library-validation
      lets the signed app load them even though they are signed by a different team.
    -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
  </dict>
</plist>
