packages:
  - "design"
  - "apps/web"
# Note: bindings/wasm is NOT a JS workspace package — its browser bundle is
# generated by wasm-pack into bindings/wasm/pkg/ and imported by apps/web via a
# relative path (see apps/web/README.md).
