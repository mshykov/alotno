> Baseline: `MSH/docs/seo.md` (org common rules). Below: Alotno-specific rules.

# SEO (Alotno)

- Only the marketing/landing site (Astro → Cloudflare Pages at `alotno.app`) is
  indexable content. The converter is an app surface, not SEO content.
- Marketing pages carry per-route metadata + a sitemap; follow the org SPA
  checklist.
